# 🚀 Webアプリケーション開発 中級編 理解度チェック（問題と回答）

中級編の理解度チェック課題のクリア【理解度チェック問題の提出必須】

SNSアプリ **「RaiseTimeLine」** の開発を通じて身につけるべき、Webアプリケーションの**中級知識の理解度を確認する問題集**と、その回答（回答者: 根本）を1つにまとめたもの。

回答は、SNSアプリ「RaiseTimeLine」の実装を確認しながら、1問ずつまとめている。

---

## 🔐 第1章: 認証・認可とセキュリティ（8問）

### Q1. セッション認証とトークン認証

#### 📝 問題

> 「セッション認証」と「トークン（JWT）認証」の違いを説明してください。
>
> また、このアプリでJWTを採用した理由（利点）も述べてください。

#### ✅ 回答

**違い**
セッション認証は、ログイン情報をサーバー側に保存し、クライアントにはセッションIDだけ渡す方式。JWT認証は、ユーザー情報と有効期限を署名付きトークンにしてクライアントに持たせ、サーバーは状態を持たずに署名を検証するだけの方式。セッションはサーバーから即座に無効化しやすい一方、JWTは発行後の失効がしにくい。

**JWTを採用した理由**
1. サーバーが状態を持たないので、台数を増やしてもセッション共有が不要で、スケールしやすい。
2. React（SPA）からAPIへ、Authorizationヘッダーで送るだけなので、フロントとバックが分離した構成と相性が良い。
3. 有効期限をトークン自体に持たせられる。
4. アクセストークンはヘッダーで送るため、Cookieのように自動送信されず、この経路にはCSRF対策のトークンが不要になる。ただし、リフレッシュトークンはCookieで送るため、`/api/auth/*` はCSRFの対象になり得る。この経路は `SameSite=Lax` で守っている（下記「このアプリでの実装」を参照）。

**このアプリでの実装**
`SessionCreationPolicy.STATELESS` でサーバーにセッションを持たせず、Authorizationヘッダーで送られたJWTを、`JwtAuthenticationFilter` で検証している。JWTは発行後に失効しにくい弱点があるため、アクセストークンを15分と短くし、長期用のリフレッシュトークンはDBで管理して、失効できるようにしている。リフレッシュトークンはhttpOnly Cookieで往復させるため、CSRF対策として `SameSite=Lax` を付け、送信先も `/api/auth` に限定している。アクセストークンの経路（Authorizationヘッダー）は自動送信されないので、CSRFの影響を受けない。

---

### Q2. アクセストークンとリフレッシュトークン

#### 📝 問題

> 以下について説明してください：

- なぜトークンを2種類に分けるのか
- アクセストークンの有効期限が短い理由

（アクセストークン: 15分 / リフレッシュトークン: 7日）

#### ✅ 回答

**2種類に分ける理由**
アクセストークンはAPI呼び出しのたびに送られ、盗まれる機会が多い。JWTは発行後にサーバーから失効させにくいため、有効期限を15分と短くして、被害を限定する。ただし、短いだけでは頻繁に再ログインが必要になり、ユーザビリティが下がる。そこで、長寿命（7日）のリフレッシュトークンで、新しいアクセストークンを自動的に取り直す仕組みにし、安全性と使いやすさを両立している。

**リフレッシュトークンの守り方**
長命なぶん、次の対策で守っている。

| 対策 | 内容 |
|---|---|
| httpOnly Cookie | JavaScriptから読めず、XSSがあっても値そのものは読み取られない（窃取を防ぐ対策。XSSがあると、スクリプトから `/api/auth/refresh` を呼ぶことはできるため、悪用まで防ぐものではない） |
| SameSite=Lax | 他サイトから発生したPOSTなどのリクエストにはCookieが付かず、CSRFを防ぐ |
| 送信先を限定 | Cookieの `path` を `/api/auth` にして、他のAPIには付かない |
| DBで管理 | `revoked_at` で、サーバー側から失効できる |
| ハッシュ化して保存 | DBが漏れても、そのままは使われない（SHA-256） |
| ローテーション | 更新のたびに新しいトークンに交換し、古いものは失効する |
| 盗用検知 | 失効済みのトークンが再提示されたら、そのユーザーの全セッションを失効する |

---

### Q3. パスワードのハッシュ化

#### 📝 問題

> 「ハッシュ化」と「暗号化」の違いを説明してください。
>
> また、なぜパスワード保存にハッシュ化を使うのか述べてください。

#### ✅ 回答

**ハッシュ化と暗号化の違い**
暗号化は、鍵を使えば元のデータに復元できる（双方向）。ハッシュ化は、元に戻せない一方向の変換で、鍵も不要。

**パスワード保存にハッシュ化を使う理由**
サービス側に必要なのは、パスワードの中身ではなく、入力が登録時と同じかどうかの確認だけだから。復元できる暗号化で保存すると、DBと鍵が漏れたときに、全員のパスワードが平文に戻り、他サービスとの使い回しも含めて、被害が広がる。ハッシュ化なら、漏れても元のパスワードは分からず、運営者も見られない。

**このアプリでの実装**
`BCryptPasswordEncoder` を使い、登録時は `encode()` でハッシュだけを保存し、ログイン時は `matches()` で入力とハッシュを照合している。BCryptは、ソルト（ハッシュ化のたびにランダム値を混ぜる）により、同じパスワードでも異なるハッシュになり、計算を意図的に遅くして、総当たりを困難にしている。

---

### Q4. 認可（Authorization）

#### 📝 問題

> 「認証」と「認可」の違いを説明し、
>
> 「自分の投稿のみ編集・削除できる」機能がどちらに該当するか答えてください。

#### ✅ 回答

**認証と認可の違い**
認証は「あなたは誰か」を確認すること（ログイン、JWTの検証）。認可は、認証された人に「何をしてよいか」の権限を確認すること。会社のビルでいえば、社員証で本人確認して入館するのが認証で、入館後に自分の部署のフロアにしか入れないのが認可。

**「自分の投稿のみ編集・削除できる」機能**
認可に該当する。ログイン済みで本人と分かっていても、その投稿の持ち主でなければ、操作は許可されないため。

**このアプリでの実装**
認証は `JwtAuthenticationFilter` と `SecurityConfig` が担当し、失敗すると401を返す。認可は `PostService.verifyOwner()` が、投稿者IDとログイン中のユーザーIDを比較して担当し、他人の投稿なら403を返す。

---

### Q5. CORS設定の実務理解

#### 📝 問題

> 以下について説明してください：

- 本番環境でのCORS設定の変更点
- `allowedOrigins("*")`の問題点

#### ✅ 回答

**本番環境でのCORS設定の変更点**
許可オリジンを、開発用の `localhost` から、本番の配信ドメイン（例：`https://app.example.com`）だけに絞る。コードに埋め込まず、環境変数 `CORS_ALLOWED_ORIGINS` で、環境ごとに切り替える。`localhost` が残っていると、開発用のオリジンから、認証情報つきでAPIを呼べてしまうため。

**`allowedOrigins("*")` の問題点**
どのオリジンからでも許可する設定で、CORSの許可リストの意味がなくなる。悪意あるサイトのJavaScriptが、ログイン中のユーザーのブラウザを踏み台にしてAPIを呼び、本人向けの情報（メールアドレスなど）を読み取れてしまう。また、このアプリは、リフレッシュトークンをCookieで往復させるため `allowCredentials(true)` にしているが、認証情報つきでは `"*"` は使えない。そのため、`setAllowedOriginPatterns` で具体的なパターンを指定している。

---

### Q6. SQLインジェクション

#### 📝 問題

> SQLインジェクションとは何か説明してください。
>
> また、MyBatisで防ぐための実装上の注意点を述べてください。

#### ✅ 回答

**SQLインジェクションとは**
入力値にSQLの断片を紛れ込ませ、開発者が想定していないSQLを実行させる攻撃。入力値をSQL文字列に直接つなげて組み立てていると、入力値が、データではなくSQLの一部として解釈される。たとえば `' OR '1'='1` を入力されると、条件が常に真になり、全ユーザーの情報が返る。被害は、情報漏えい、データの改ざん・削除、認証の回避などにおよぶ。

**MyBatisで防ぐための注意点**
値の埋め込みには `#{}` を使う。`#{}` は、プレースホルダ（`?`）として値を別に渡すため、入力値は常にデータとして扱われ、SQLとして解釈されない。`${}` は、値を文字列として直接埋め込むため、SQLインジェクションの原因になる。テーブル名や並び順の列名など、どうしても `${}` が必要な場合は、入力値をそのまま使わず、許可リストで検証してから使う。

**このアプリでの実装**
Mapperに `${}` は使っておらず、ユーザー検索も `username ILIKE #{pattern}` のように、`#{}` で値を渡している。

---

### Q7. XSS（クロスサイトスクリプティング）

#### 📝 問題

> 以下について説明してください：

- `<script>alert('攻撃')</script>` が投稿された場合のリスク
- Reactでの対策方法

#### ✅ 回答

**`<script>alert('攻撃')</script>` が投稿された場合のリスク**
入力値がエスケープされずに表示されると、その投稿を見た他のユーザーのブラウザで、スクリプトが実行されてしまう（XSS）。`alert` は無害な例だが、悪意あるコードなら、トークンの窃取、ユーザーになりすました投稿・削除、偽のログイン画面によるパスワード窃取、別サイトへのリダイレクトなどができる。SNSは、ユーザーの入力を他のユーザーが見る仕組みなので、被害が広がりやすい。

**Reactでの対策方法**
Reactは、`{post.body}` のように書けば、文字列を自動でエスケープし、タグとして解釈せず、ただの文字列として表示する。ユーザー入力に `dangerouslySetInnerHTML` を使うと、エスケープが無効になるため、使わない。

**このアプリでの実装**
投稿とコメントの本文は `{post.body}` `{comment.body}` で表示しており、`dangerouslySetInnerHTML` は使っていない。さらに、万一XSSが起きても被害を小さくするため、アクセストークンは、localStorageに置かず、メモリだけで保持し、リフレッシュトークンは、JSから読めないhttpOnly Cookieにしている。これらは、トークンの窃取（持ち出し）を防ぐ対策であり、XSSそのものを防ぐものではない。XSSが成立すると、ページ上のスクリプトはユーザーとして操作できてしまうため、エスケープによる予防が第一の対策になる。

---

### Q8. HTTPSと本番セキュリティ

#### 📝 問題

> 以下について説明してください：

- ALBを使った場合のSSL/TLS証明書の配置場所
- ALB〜バックエンド間通信のHTTPS必要性

#### ✅ 回答

**ALBを使った場合のSSL/TLS証明書の配置場所**
ALB（ロードバランサー）に置く。ブラウザからの通信を、ALBでHTTPSとして受け取り、暗号を解く（SSL終端）。バックエンドが複数台に増えても、証明書はALBの1か所で管理でき、ACM（AWS Certificate Manager）を使えば、発行・更新も自動化できる。バックエンド側は、暗号化の処理と証明書の設定が不要になる。

**ALB〜バックエンド間通信のHTTPS必要性**
ブラウザ〜ALB間（インターネット側）は、HTTPSが必須。トークンやパスワードが、通信経路上で盗み見・改ざんされるのを防ぐため。一方、ALB〜バックエンド間は、VPC（プライベートなネットワーク）内なので、一般的な構成では、HTTPでも許容される。ただし、金融・医療など、エンドツーエンドの暗号化が求められる要件がある場合や、より強い多層防御を求める場合は、内側もHTTPSにする。

**このアプリでの想定**
設計（`docs/09_infrastructure.md`）では、ブラウザ〜CloudFront間をHTTPS（`redirect-to-https`）で受け、`/api/*` をALB経由でEC2上のSpring Bootに届ける構成を想定している。ただし、現状のTerraformでは、EC2・ALB・CloudFrontは未定義で、設計上の想定である。

---

## 🏗 第2章: アプリケーションアーキテクチャ（7問）

### Q9. レイヤードアーキテクチャ

#### 📝 問題

> Controller / Service / Repository の役割を説明してください。
>
> また、以下も答えてください：

- なぜ分離するのか
- ControllerとServiceの責務の境界

#### ✅ 回答

**各層の役割**
- **Controller**: HTTPの入口と出口。リクエストを受け取り、入力値の形式を検証（`@Valid`）し、Serviceを呼び、結果をHTTPレスポンスにして返す。
- **Service**: 業務のルール（ビジネスロジック）を担当する。「投稿者本人か」などの判断や、複数の処理をまとめるトランザクション管理を行う。
- **Repository（このアプリでは、MyBatisのMapper）**: DBとのやりとりだけを担当し、SQLを実行する。

**なぜ分離するのか**
層どうしの依存が減り、疎結合になるため。①テストしやすくなる（Serviceだけを、Mapperをモックにして検証できる）、②変更の影響範囲が小さくなる（DBやAPI形式を変えても、業務ロジックに触れずに済む）、③業務ロジックを別の入口からも再利用できる、④1クラスの役割が明確で読みやすくなる。

**ControllerとServiceの責務の境界**
Controllerは、HTTPに関わること（URL、ステータスコード、入力値の形式チェック）を担当し、業務ルールの詳細は知らない。Serviceは、業務ルールを担当し、HTTPのことは知らない。目安は、「HTTP以外の入口から呼ばれても必要なルールか」で、必要ならService。このアプリでは、`PostController.delete` は `postService.delete()` を呼んで204を返すだけで、「持ち主か」の判断は `PostService.verifyOwner()` が行い、例外を `GlobalExceptionHandler` が403に変換している。

---

### Q10. DTOパターン

#### 📝 問題

> エンティティを直接APIに使わない理由を説明してください。

#### ✅ 回答

**エンティティを直接APIに使わない理由**
エンティティは、DBのテーブルの構造そのものであり、APIの入出力にそのまま使うと、次の問題が起きるため、専用のDTOを間に挟む。

1. **情報の漏えい**（出力）: `User` には `passwordHash` が含まれ、メールアドレスは本人にだけ返すべき情報。エンティティをそのまま返すと、これらが漏れる。DTOなら、返す項目を明示的に選べる。
2. **意図しない書き換え**（入力）: エンティティで受け取ると、クライアントが、IDや登録日時など、本来指定できない項目を勝手に書き換えられる（マスアサインメント）。入力用DTOには、指定してよい項目だけを定義する。
3. **疎結合にできる**: DBのカラムやテーブルの構造を変えても、DTOが間にあるため、APIの形は変わらず、フロントエンドに影響しない。
4. **画面に必要な形にできる**: 自分がいいね済みか（`likedByMe`）や、閲覧用のURLに変換したアバター画像など、DBにない値も追加できる。

**このアプリでの実装**
`UserResponse`（メールアドレスを含み、本人にだけ返す）と `ProfileResponse`（含まない、他人向け）を使い分け、`passwordHash` は、どのDTOにも含めていない。入力側の `PostRequest` は、`body` だけを受け取る。

---

### Q11. グローバル例外ハンドリング

#### 📝 問題

> 各Controllerでtry-catchする場合と比較した利点を説明してください。

#### ✅ 回答

**各Controllerでtry-catchする場合と比較した利点**
例外の処理を、`GlobalExceptionHandler`（`@RestControllerAdvice`）の1か所にまとめることで、次の利点がある。

1. **コードの重複がなくなる**: 各Controllerに「例外 → 403」などの変換を書かなくてよい。
2. **可読性が上がる**: Controllerは、本来の処理だけを書ける（`PostController.delete` は、`postService.delete()` を呼んで204を返すだけの3行）。
3. **エラー形式を統一できる**: 共通の `ErrorResponse`（`message` と、入力エラー用の `fieldErrors`）で返すため、フロントエンドは、どのAPIのエラーも、同じ処理で表示できる。
4. **書き忘れによる不統一を防げる**: 新しいControllerを追加しても、エラーの扱いが自動的に揃う。
5. **内部情報を守れる**: 想定外の例外（500）は、原因をログにだけ残し、応答には汎用のメッセージしか返さない。テーブル名やSQLなどの内部情報が、攻撃者に漏れない。

**このアプリでの実装**
Serviceは、業務上のエラーを例外として投げるだけで、HTTPを意識しない。`GlobalExceptionHandler` が、`PostForbiddenException` を403、`PostNotFoundException` を404、`InvalidCredentialsException` を401などに変換する。

---

### Q12. MyBatis vs JPA

#### 📝 問題

> 以下の観点で違いを説明してください：

- SQL制御
- 学習コスト

#### ✅ 回答

**SQL制御**
MyBatisは、SQLを自分で書くため、複雑な検索条件や性能の調整を細かく制御でき、実行されるSQLも明確。JPAは、SQLがオブジェクト操作から自動生成されるため、単純なCRUDは少ない記述で済むが、複雑な検索や性能調整は難しく、意図しないSQLが発行される場合もある（N+1問題など）。

**学習コスト**
MyBatisは、SQLの知識があれば書けるため、学習コストが低い。JPAは、エンティティの関連、キャッシュ、遅延読み込みなど、ORMの仕組みを理解する必要があり、学習コストが高い。一方、単純なCRUDを大量に書く場合は、JPAのほうが記述量は少なくて済む。

**このアプリでの選択**
「フォロー中の投稿を、新しい順に、カーソルで区切って取得する」という複雑な検索（`findTimeline`）があり、サブクエリで先に1ページ分に絞ってからJOINするなど、SQLを自分で調整する必要があるため、MyBatisを採用している。

---

### Q13. 環境ごとの設定管理

#### 📝 問題

> プロファイルの仕組みと、環境ごとに設定を分ける理由を説明してください。

#### ✅ 回答

**プロファイルの仕組み**
Spring Bootは、共通の `application.properties` に加えて、指定したプロファイル名（`dev`、`prod` など）に対応する `application-{プロファイル名}.properties` を読み込み、共通の設定を上書きする。起動時に `SPRING_PROFILES_ACTIVE` で、どの環境かを指定して切り替える。

**環境ごとに設定を分ける理由**
開発と本番で、CORSの許可オリジン、JWTの署名鍵、DBの接続先、Cookieの `Secure` 属性など、値が異なる設定があるため。
1. 同じコード（同じビルド成果物）を、どの環境でも動かせ、開発・テストで確認した動作を、本番でも再現できる（コードを書き換える手間もミスもなくなる）。
2. 開発用の設定（`localhost` の許可や仮の署名鍵）が、本番に紛れ込むのを防げる。
3. 秘密の情報を、コードに直接書かずに済む。

**このアプリでの実装**
`application.properties` に、`${CORS_ALLOWED_ORIGINS:http://localhost:5173,...}` のように、環境変数と開発用の初期値を書いている。開発では初期値で動き、本番では環境変数で値を差し替える。テストには `application-test.properties` を用意している。ただし、初期値が公開されたコードに書かれているため、本番で `JWT_SECRET` などを設定し忘れると、エラーにならず、開発用の値で起動してしまう。本番では、必須の環境変数を、必ず設定する運用が必要。

---

### Q14. Flyway（DBマイグレーション）

#### 📝 問題

> マイグレーションツールを使う理由を説明してください。
>
> （直接ALTER TABLEしない理由）

#### ✅ 回答

**マイグレーションツールを使う理由（直接ALTER TABLEしない理由）**
DBの構造の変更を、番号つきのSQLファイルとして記録し、順番に自動で適用するため。直接 `ALTER TABLE` を手で実行すると、次の問題が起きる。

1. **環境ごとにDBの構造がバラバラになる**: 手で実行した変更は、他の開発者のDBやテスト・本番のDBに自動では反映されず、コードが期待する構造とDBがずれる。
2. **履歴が残らない**: いつ、誰が、なぜ変更したのか分からず、トラブルシューティングできない。
3. **再現できない**: 新しい環境を、ゼロから最新の状態にできない。

Flywayなら、①アプリの起動時に、未適用のSQLが番号順に自動で適用され、どの環境でも同じ構造になる。②SQLファイルをGitで管理でき、PRでレビューもできるため、履歴が残る。③V1から順に流せば、最新の状態を再現できる。適用済みの番号は `flyway_schema_history` に記録され、二重に実行されず、適用済みのファイルを書き換えると、チェックサムの不一致でエラーになる。そのため、変更したいときは、既存のファイルを直さず、新しい番号のファイルを追加する。

**このアプリでの実装**
`db/migration/` に、`V1__create_users.sql` から `V8__create_post_images.sql` まで、番号順にSQLを管理し、`spring.flyway.enabled=true` で、起動時に自動適用している。

---

### Q15. Axiosインターセプター

#### 📝 問題

> インターセプターとは何か説明してください。
>
> また、これが無い場合の問題点も述べてください。

#### ✅ 回答

**インターセプターとは**
Axiosで、リクエストを送る直前、またはレスポンスを受け取った直後に、共通の処理を自動で差し込む仕組み。

**これが無い場合の問題点**
1. **リクエスト側**: 投稿、コメント、いいねなど、すべてのAPI呼び出しに、`Authorization` ヘッダーを手で付ける必要があり、コードが重複する。1か所でも付け忘れると、そのAPIだけが401になる。
2. **レスポンス側**: アクセストークンが切れて401が返ったときの「リフレッシュトークンで取り直して再送する」処理を、画面ごとに書く必要がある。書き忘れた画面では、トークンの有効期限（15分）ごとに、突然エラーになり、ユーザーが何度もログインし直すことになる。

**このアプリでの実装**
`api/client.ts` に、2つのインターセプターがある。リクエスト側は、全リクエストに `Authorization: Bearer ...` を自動で付ける。レスポンス側は、401を受けたら、リフレッシュトークンでアクセストークンを取り直し、元のリクエストを1度だけ再送する。再送は1回だけにして、無限ループを防ぎ、同時に複数の401が来ても、リフレッシュは1本にまとめている（リフレッシュトークンのローテーションで、正規のユーザーが強制ログアウトされるのを防ぐため）。

---

## 🎨 第3章: フロントエンド設計（6問）

> **この章について**
>
> このアプリはAIを活用してフロントエンドを実装しています。
>
> 目的は「自力実装」ではなく、
>
> 👉 **AIの設計意図を理解し、良し悪しを判断できる力** を問うことです。

---

### Q16. React Context

#### 📝 問題

> なぜpropsではなくContextが選ばれたのか説明してください。
>
> また、適切なケース / 不適切なケースも述べてください。

#### ✅ 回答

**なぜpropsではなくContextが選ばれたのか**
ログイン中のユーザー情報は、ヘッダー、投稿カード、コメント、ルートの出し分けなど、画面のあちこちで必要になる。propsで渡すと、間のコンポーネントが、自分では使わない値を受け取って下へ渡すだけの「バケツリレー」になり、コードが冗長になって、値の追加・変更のたびに、間のすべてのコンポーネントを修正することになる。Contextなら、途中の階層を飛び越えて、必要な場所で `useAuth()` を呼ぶだけで、直接受け取れる。

**適切なケース**
画面のあちこちで必要で、あまり頻繁には変わらない、アプリ全体の値（ログイン中のユーザー、テーマ、言語設定など）。

**不適切なケース**
1か所、または近くの数か所でしか使わない値（入力中のフォームの文字、コメント欄の開閉など）は、`useState` やpropsのほうが、値の流れが分かりやすい。また、頻繁に変わる値をContextに入れると、その値を使うすべてのコンポーネントが再描画され、無駄が増える。

**このアプリでの実装**
`auth/authContext.ts` で、ログイン中のユーザー情報（`user`）、状態（`status`）、`login` / `logout` などをContextとして共有し、`useAuth()` で、`PostCard`、`CommentCard`、`Header`、`RouteGuards` などから使っている。

---

### Q17. カスタムフック

#### 📝 問題

> なぜロジックをコンポーネントではなくフックに分離するのか説明してください。

#### ✅ 回答

**なぜロジックをコンポーネントではなくフックに分離するのか**
1. **重複をなくせる**: タイムラインと、ユーザーの投稿一覧のように、同じロジック（カーソルでの続きの読み込み）が複数の画面で必要になったとき、コンポーネントに直接書くと、コピーが増える。フックに分離すれば、1か所を直すだけで、すべてに反映される。
2. **画面とロジックの依存を減らせる（疎結合）**: コンポーネントは「画面の表示」、フックは「データの取得や状態の管理」と、役割を分けることで、見た目の変更と、ロジックの変更が、互いに影響しにくくなる。コンポーネントも短く読みやすくなる。
3. **テストしやすい**: 画面を描画せずに、フックだけで、データ取得や状態管理の動作を確認できる。

**このアプリでの実装**
`useCursorPager` に、カーソルでの続きの読み込み、二重取得の防止、追い越したレスポンスの破棄などの共通のロジックを集約し、`useTimeline`、`useUserPosts` は、取得先ごとのAPIとの橋渡しだけを担当している。`HomePage` や `ProfilePage` は、フックを呼ぶだけで、画面の表示に集中できる。

---

### Q18. コンポーネント設計

#### 📝 問題

> 良いコンポーネント分割とは何か、具体例を挙げて説明してください。

#### ✅ 回答

**良いコンポーネント分割とは**
1つのコンポーネントに、1つの役割を持たせ、意味のあるまとまりで分割すること。

**理由**
1. **変更の影響が狭くなる（疎結合）**: 全部を1つに書くと、1か所を直したときに、関係のない部分が壊れる可能性が出る。役割ごとに分けておけば、影響が、その部品の中に収まる。
2. **再利用でき、重複がなくなる**: 複数の場所で使う部品を分けておけば、ルールの変更を、1か所直すだけで、すべてに反映できる。
3. **読みやすく、テストしやすい**: 1つの部品が短く、役割が明確になる。

ただし、分けすぎると、部品を追いかけにくくなり、propsの受け渡しも増えるため、「再利用されるか」「まとまった意味があるか」を基準にする。

**具体例（このアプリ）**
`Avatar` は、画像がなければ、ユーザー名から決まる色と頭文字で代用する、というルールを持つ部品で、`PostCard`、`CommentCard`、`Header`、`ProfilePage` など、8か所以上で再利用されている。`PostCard` は、`Avatar`（画像）、`AuthorLink`（投稿者へのリンク）、`PostActions`（いいね・コメントのボタン）を組み合わせて作られており、それぞれが1つの役割だけを持つ。

---

### Q19. TypeScriptの型安全性

#### 📝 問題

> 型のメリットと、AIコードレビュー時の利点を説明してください。

#### ✅ 回答

**型のメリット**
1. **動かす前にミスに気づける**: スペルミス、`null` の見落とし、引数の種類の間違いなどを、プログラムを動かす前（コンパイル時）に、エラーとして検出できる。型が無いと、ユーザーが画面を開いたときに、初めて壊れる。
2. **型が、コードの説明（ドキュメント）になる**: 値の形が分かるので、読みやすく、変更もしやすい。
3. **エディタの補完やリファクタリングが効く**: 名前を変えたときの影響範囲も、機械的に洗い出せる。

**AIコードレビュー時の利点**
AIは、APIの応答の形を勘違いしたり、`null` の可能性を見落としたりするミスをしやすい。型があれば、そうした機械的に確認できるミスは、コンパイラが自動で弾いてくれる。人間のレビュアーは、形の確認に時間をかけず、設計や業務ロジックが正しいかの判断に集中できる。また、型を見れば、データの形が分かるので、レビューにかかる時間も短くなる。

**このアプリでの実装**
`tsconfig` で `strict` と `noUncheckedIndexedAccess` を有効にし、`any` は使っていない。APIの型は、`openapi-typescript` で、バックエンドのAPI仕様書（`openapi.json`）から自動生成しているため、バックエンドとフロントエンドの間で、形のずれが起きたら、型のエラーで気づける。

---

### Q20. SPAルーティング

#### 📝 問題

> 従来のWebとの違いを説明し、
>
> 「直接URLで404になる問題」の原因と対策を述べてください。

#### ✅ 回答

**従来のWebとの違い**
従来のWeb（MPA）は、画面を切り替えるたびに、サーバーへリクエストを送り、URLごとに用意されたHTMLを受け取って、ページ全体を読み込み直す。SPAは、最初に1枚のHTMLとJavaScriptを読み込み、その後の画面の切り替えは、サーバーへリクエストを送らず、JavaScript（React Router）が、ブラウザの中で、URLを書き換えて、表示する画面を切り替える。画面遷移が速く、体感が滑らかになる。

**「直接URLで404になる問題」の原因**
`/users/alice` のようなパスは、JavaScriptの中でだけ存在するもので、サーバー（S3）には、対応するファイルが無い。リンクからの遷移は、サーバーにリクエストが行かないため問題ないが、URLの直接入力やリロードでは、ブラウザが、そのパスをサーバーにリクエストするため、404（S3では403）になる。

**対策**
存在しないパスへのリクエストには、`index.html` を返す（フォールバック）ようにする。ブラウザは、`index.html` とJavaScriptを読み込み、React Routerが、URLを見て、正しい画面を表示する。AWSでは、CloudFrontのカスタムエラーレスポンスで、403/404を `/index.html`（200）に置き換える。開発時は、Viteの開発サーバーが、自動で行う。フォールバックのあと、存在しないURLの扱いは、React Routerのルート定義（このアプリでは、`path="*"` でホームへ移動）が担う。

---

### Q21. 楽観的UI更新

#### 📝 問題

> 以下を比較してください：

- サーバー応答後に更新
- 楽観的更新

そしてどちらが適切か理由付きで答えてください。

#### ✅ 回答

**サーバー応答後に更新**
ボタンを押したらリクエストを送り、サーバーから成功の応答が返ってから、画面に反映する。画面が、常にサーバーの状態と一致し、実装もシンプル。一方、通信の遅延ぶん、画面が変わるまで待たされる。

**楽観的更新**
ボタンを押したら、すぐに画面へ反映し、裏でリクエストを送る。成功ならそのまま、失敗なら元に戻す。体感速度が速い。一方、成功すると見込んで更新するため、失敗したときに、ユーザーの認識とずれが起こり、巻き戻しの処理も必要になる。

**どちらが適切か**
処理の性質で選ぶ。「いいね」のように、失敗しても影響が小さく、成功する確率が高い処理は、楽観的更新が向いている。投稿の削除や編集、決済のように、失敗したときの影響が大きく、サーバーの判断が重要な処理は、サーバーの応答後に更新するほうが適切。また、いいねの件数のように、他のユーザーの操作でも変わり、サーバーの値を正確に見せたい場合は、応答後に更新するほうが、ずれない。

**このアプリでの実装**
いいねは、サーバーの応答後に更新する方式。`handleToggleLike` が、APIを呼んで、応答（`likeCount` / `likedByMe`）が返ってから、画面を更新する。処理中はボタンを無効にして、二重送信を防ぎ、失敗したときは、エラーを表示するだけで、画面は元のままなので、巻き戻しの処理が要らない。

---

## ☁️ 第4章: インフラとデプロイ（5問）

### Q22. Docker / docker-compose

#### 📝 問題

> Dockerの概要と、ローカルインストールとの違いを説明してください。

#### ✅ 回答

**Dockerの概要**
アプリと、その動作に必要なもの（実行環境、ライブラリ、設定など）を、コンテナという1つの隔離された環境にまとめて動かす仕組み。コンテナは、仮想マシンよりも軽く、起動も速い。複数のコンテナ（DB、ストレージ、バックエンドなど）は、docker-composeで、まとめて定義し、1コマンドで起動できる。

**ローカルインストールとの違い**
ローカルインストールは、各自のPCに、Java、PostgreSQLなどを、直接インストールして設定する。OSやバージョン、設定が、開発者ごとに異なり、「自分のPCでは動くのに、他の人のPCでは動かない」問題が起きやすい。新しく参加した開発者は、必要な環境を、すべて自分で整える必要がある。Dockerなら、イメージ（設計図）から、誰の環境でも、同じ構成のコンテナが立ち上がり、環境の差を吸収できる。手順も、`docker-compose up -d` で済み、開発者の負担が少ない。

**このアプリでの実装**
`docker-compose.yml` に、`db`（`postgres:15`）、`minio`（S3互換のストレージ）、`minio-init`（バケットの自動作成）、`backend` を定義している。イメージのバージョンを固定し、`depends_on` と `healthcheck` で、DBの準備ができてから、バックエンドを起動する。`TZ: Asia/Tokyo` で、時刻をそろえ、`volumes` で、DBのデータを永続化している。バックエンドの `Dockerfile` は、ビルド用と実行用の2段階（マルチステージ）にして、実行用のイメージには、できあがったjarだけを入れている。

---

### Q23. AWS基本サービス

#### 📝 問題

> EC2 / RDS / ALB / S3 の役割と分離理由を説明してください。

#### ✅ 回答

**各サービスの役割**
- **EC2**: AWS上の仮想サーバー。このアプリでは、バックエンド（Spring Boot）のAPIを動かす。
- **RDS**: AWSが管理するデータベースのサービス。ユーザー、投稿、コメントなどのデータ（PostgreSQL）を保存する。
- **ALB**: リクエストを、後ろの複数のサーバーに振り分けるロードバランサー。`/api/*` を、EC2に振り分け、HTTPSの終端やヘルスチェックも担当する。
- **S3**: ファイルを保存するストレージ。投稿画像やプロフィール画像の本体を保存し（DBには `s3_key` だけを持つ）、Reactのビルド成果物の配信元にもなる。

**分離する理由**
役割ごとに分離することで、疎結合になり、変更や障害の影響範囲が狭まり、それぞれを個別に増やしたり、入れ替えたりできる。
- **EC2とRDS**: DBをEC2の外に出すことで、EC2を増やしたり、入れ替えたりしても、データは残る。複数のEC2が同じDBを見られるため、データがバラバラにならない。バックアップや障害時の切り替えも、AWSに任せられる。
- **ALBとEC2**: EC2を複数台に増やせ、1台が故障しても、生きている台にだけ振り分けられる。HTTPSの証明書も、ALBの1か所で管理できる。
- **S3とEC2・DB**: 大きく、増え続ける画像をS3に置くことで、容量や負荷を気にせず、安く保存でき、複数のEC2から同じ画像を参照できる。

**このアプリでの想定**
設計（`docs/09_infrastructure.md`）では、ブラウザ → CloudFront → `/api/*` は、ALB → EC2（Spring Boot） → RDS（PostgreSQL）、画像はS3、という構成を想定している。ただし、現状のTerraformでは、S3とIAMだけが定義済みで、EC2・RDS・ALBは未着手である。

---

### Q24. IaC（Terraform）

#### 📝 問題

> 手動構築との違いとメリットを説明してください。

#### ✅ 回答

**手動構築との違い**
手動構築は、AWSのコンソール（Web画面）で、人がクリックしながら、リソースを1つずつ作る方法。IaC（Terraform）は、インフラの構成をコード（`.tf`）で書いて、`terraform apply` で、その内容どおりに作成・変更する方法。

**IaCのメリット**
1. **再現性**: 手動は、手順を人の記憶に頼るため、同じ環境を再現しにくく、設定の見落としで環境ごとに違いが出る。コードなら、何度実行しても同じ環境が立ち上がる。開発・テスト・本番の同じ構成も、簡単に作れる。
2. **履歴とレビュー**: コードをGitで管理できるので、いつ、誰が、なぜ変更したかが残り、PRでレビューもできる。手動だと、設定の履歴が残らない。
3. **確実に削除できる**: 不要になった環境をまとめて削除でき、リソースの消し忘れ（課金され続けること）が起きにくい。
4. **設定の意図を残せる**: コメントで、なぜその設定にしたのかを、コードの隣に書ける。

コンソールで直接変更すると、コードと実際のインフラがずれ（ドリフト）、次の `terraform apply` で上書きされる恐れがあるため、変更は必ずコードから行う。

**このアプリでの実装**
`terraform/` に、画像用S3バケット（`s3.tf`）とIAM（`iam.tf`）を定義している。S3は、公開アクセスの遮断、暗号化、不完全なアップロードの破棄などの設定を、コードで管理している。状態ファイル（`*.tfstate`）は、アカウントIDなどを含むため、コミットせず、`.terraform.lock.hcl` は、バージョン固定のため、追跡している。EC2・RDS・ALB・CloudFrontは、まだ未着手である。

---

### Q25. 環境変数と機密情報

#### 📝 問題

> ソースコードに直接書く問題点と、安全な管理方法を説明してください。

#### ✅ 回答

**ソースコードに直接書く問題点**
1. **漏えいの危険**: Gitにpushすると、平文のままリポジトリに残り、リポジトリを見られる人に、値が見えてしまう。
2. **履歴に残る**: あとでコードから削除しても、過去のコミットの履歴に値が残るため、一度でもコミットした値は、漏れたものとして扱い、無効にして、作り直す必要がある。
3. **環境ごとに値を変えられない**: 開発と本番で値が違うため、環境ごとにコードを書き換えることになり、ミスの原因になる。

**安全な管理方法**
機密情報は、コードに書かず、環境変数で、実行時に渡す。値を変えるときは、コードの修正が要らず、設定を変えるだけで済む。ローカルでは、`.env` ファイルに値を書き、`.gitignore` で、Gitの管理から外す。変数の名前と、ダミー値の見本は、`.env.example` として、コミットする。本番では、AWS Secrets Managerなどの専用のサービスで、機密情報を管理する。AWSへのアクセスは、アクセスキーの代わりに、EC2のIAMロールを使えば、そもそも秘密の値を持たずに済む。

**このアプリでの実装**
`application.properties` は、`${JWT_SECRET:...}` のように、環境変数から値を読み、`docker-compose.yml` も、`.env` から値を渡している。`.env` は、`.gitignore` で除外し、`.env.example` だけを、コミットしている。`.env.example` のコメントに、本番では、`JWT_SECRET` を推測不可能な値に差し替えることと、S3のキーを空にして、IAMロールを使うことを、明記している。

---

### Q26. 構造化ログ

#### 📝 問題

> JSONログの利点と、MDC（requestId / userId）の役割を説明してください。

#### ✅ 回答

**JSONログの利点**
ログを、人が読む文章ではなく、キーと値の形（構造化）で出力するため、機械で扱いやすい。Datadogなどのログ監視サービスに、そのまま取り込め、「`usr.id` が `42` で、`level` が `WARN` のログ」のように、項目を指定した検索・絞り込み・集計が簡単にできる。テキストのログは、文字列を解析（パース）する必要があり、書き方が変わると、検索が壊れやすい。

**MDC（requestId / userId）の役割**
MDCは、1回のリクエストの処理中に出る、すべてのログに、共通の情報を、自動で付ける仕組み。
- **requestId**: リクエスト1回ごとの番号。同時に多数のリクエストが処理され、ログが混ざっていても、同じリクエストのログを、まとめて追える。レスポンスヘッダー（`X-Request-Id`）にも付けるので、ユーザーからの問い合わせの調査にも使える。
- **userId**: ユーザーの番号。複数のリクエストにまたがって、そのユーザーの操作を追える。

MDCがなければ、ログの文に、毎回、手でIDを書く必要があり、書き忘れると、誰の、どのリクエストか、分からなくなる。調査は、`usr.id` でユーザーのログに絞り、失敗したリクエストの `request_id` を見つけ、その `request_id` で、そのリクエストの詳細を追う、という順で行う。

**このアプリでの実装**
`RequestLoggingFilter` が、リクエストごとに、UUIDの `request_id` を、MDCに載せ、`X-Request-Id` ヘッダーにも付ける。`JwtAuthenticationFilter` が、認証成功時に、`usr.id` を載せる。載せるのはIDだけで、ユーザー名やメールアドレスは個人情報のため出さない。キー名は、Datadogの標準属性にそろえ、本番では、1行JSONで出力する。リクエスト完了時に、MDCを `clear()` して、次のリクエストに値が残らないようにしている。

---

## 🧪 第5章: テストと品質保証（5問）

### Q27. テストの種類

#### 📝 問題

> 以下の違いと具体例を説明してください：

- 単体テスト
- 結合テスト
- E2Eテスト

#### ✅ 回答

**単体テスト**
1つのクラスやメソッドなど、小さい部品だけを、他から切り離して、確かめるテスト。依存する部品（DBや外部サービスなど）は、モックに差し替える。速く、失敗したときの原因も特定しやすい。例：`AuthServiceTest`（`AuthService` だけを、Mapperをモックにして、「パスワードをハッシュ化して保存する」「ユーザー名が重複していれば例外を投げる」を確かめる）。

**結合テスト**
複数の部品を、実際につなげて、連携が正しく動くかを確かめるテスト。単体テストでは見つけにくい、つなぎ目の問題（SQLの誤り、DBの制約、認証フィルタとの連携など）を見つけられる。単体テストよりは遅い。例：`AuthFlowIntegrationTest`（Controller → Service → Mapper → DBを、モックなしで通し、「signupでユーザーが登録され、返ったトークンで `/me` が同じユーザーを返す」を確かめる。外部のS3だけ、モックに差し替える）。

**E2Eテスト**
ブラウザなど、実際のユーザーの操作と同じ流れで、アプリ全体を、最初から最後まで確かめるテスト。画面を含めた全体の動きを保証できるが、遅く、壊れやすく、原因の特定も難しい。例：「ログイン画面で入力してログイン → 投稿する → タイムラインに表示される」を、自動で操作して確かめる。

**使い分け**
範囲が大きいほど、遅く、壊れやすいため、単体テストを多く、結合テストを適度に、E2Eテストを少なく、というバランス（テストピラミッド）で組み合わせる。DBの制約（UNIQUEなど）は、モックには存在しないため、本物のDBを通す結合テストでないと確かめられない。このアプリには、単体テスト（`service/`、`security/` など）と、結合テスト（`integration/`）がある。E2Eテストは、現状、見当たらない。

---

### Q28. モック（テストダブル）

#### 📝 問題

> モックの利点と注意点を説明してください。

#### ✅ 回答

**モックとは**
テストのときだけ、本物の代わりに使う、偽物の部品。あらかじめ決めた値を返したり、呼ばれたかを記録したりする。

**モックの利点**
1. **準備の手間が減る**: 本物のDBやS3を使うと、データの用意や接続の設定が必要。モックなら不要。
2. **速く、安定して動く**: 外部サービスの遅延、障害、料金に左右されない。
3. **再現しにくい状況を作れる**: 「S3がエラーを返した」「例外が起きた」など、本物では意図的に起こしにくい状況を、簡単に作って、アプリの反応を確かめられる。
4. **確かめたい部品だけを切り離せる**: 単体テストで、1つのクラスのロジックだけを確かめられる。

**注意点**
1. **本物の動きを保証しない**: モックは、本物のDBのように、制約（UNIQUEなど）やSQLの結果を持たない。モックで通ったテストが、本物でも動く保証にはならない。本物の仕様が変わっても、モックが古いままだと、テストは通るのに、本番で壊れる。
2. **使いすぎると、確かめたい部分まで偽物になる**: モックの設定どおりに呼んでいるかを確かめているだけになり、実際の動きを確かめられない。
3. **実装の細部に縛られる**: 呼び出しの回数や順番を細かく確かめすぎると、内部の書き方を変えただけで、テストが壊れる。
4. **対策**: 単体テスト（モックあり）と、結合テスト（本物のDBなどを通す）を、組み合わせる。結合テストでも、S3のような外部サービスだけは、モックに差し替える（テストから制御しにくく、遅延・課金・不安定さがあるため）。

**このアプリでの実装**
`AuthServiceTest` などの単体テストでは、Mockitoで、Mapperをモックにして、Serviceのロジックだけを確かめている。`S3StorageServiceTest` では、`thenThrow` で、S3のエラーを再現している。結合テスト（`IntegrationTestBase`）では、Controller → Service → Mapper → H2を、モックなしで通し、外部サービスのS3だけを、`@MockitoBean` で差し替えている。

---

### Q29. 境界値とエッジケース

#### 📝 問題

> 「1〜280文字制約」のテストケースを境界値分析で説明してください。

#### ✅ 回答

**境界値分析とは**
有効な入力と、無効な入力の、切り替わる境目（境界）と、そのすぐ内側・外側の値を、テストする方法。プログラムの誤りは、`<` と `<=` の書き間違いなど、境界で起きやすいため、代表値だけでは気づけない誤りを見つけられる。

**「1〜280文字」のテストケース**
まず、同値分割で、「無効（0文字）」「有効（1〜280文字）」「無効（281文字以上）」に分ける。その上で、2つの境界の、内側と外側をテストする。

| 境界 | 入力 | 期待する結果 |
|---|---|---|
| 下の境界 | 0文字 | 拒否 |
| | 1文字 | 受け付ける |
| 上の境界 | 280文字 | 受け付ける |
| | 281文字 | 拒否 |

内側（280）のテストは、上限が厳しすぎる誤り（`<` と `<=` の書き間違いなど）を、外側（281）のテストは、上限がゆるすぎる誤りを、見つける。片方だけでは、反対向きの誤りを見落とす。

**エッジケース**
空白だけ、改行、絵文字（`String.length()` では2文字と数えられることがある）など、数え方が問題になる入力も確かめる。

**このアプリでの実装**
`PostControllerTest` で、`"あ".repeat(280)`（受け付ける）と、`"あ".repeat(281)`（400）の、上の境界の2点をテストしている。下の境界は、「本文が空でも、画像があれば投稿できる」という仕様のため、「本文が空で、画像も無い投稿は400になる」というテストで確かめている。

---

### Q30. テストカバレッジ

#### 📝 問題

> カバレッジの意味と限界を説明してください。
>
> 「100%＝バグなし」は正しいかも答えてください。

#### ✅ 回答

**カバレッジの意味**
テストを実行したときに、コードの、どれだけの割合が、実行されたかを示す指標。行カバレッジ（実行された行の割合）や、分岐カバレッジ（`if` などの真・偽の両方が実行された割合）などがある。テストされていない箇所を、見つけるための道具になる。

**カバレッジの限界**
1. **書かれていない処理は測れない**: 書き忘れた処理や、仕様の抜けは、100%でも見つからない。
2. **「実行された」と「正しさを確かめた」は別**: 結果を確認しないテスト（アサーションなし）でも、コードが実行されれば、カバレッジは上がる。
3. **入力の組み合わせや、境界の誤りは見つからない**: `<=` と `<` の書き間違いなどは、その行が実行されていれば、100%でもバグが残る。
4. **数値を目標にすると、意味のないテストが増える**。

**「100%＝バグなし」は正しいか**
正しくない。100%は、「テストが、コードをすべて通った」ことを示すだけで、「正しく動く」ことの保証ではない。書き忘れた処理などは、100%の外側に、バグとして残り得る。カバレッジは、品質の保証ではなく、テストが不足している場所を見つけるための目安として使う。

**このアプリでの実装**
バックエンドは、JaCoCoで、フロントエンドは、Vitestのカバレッジで測っている。どちらも、閾値でビルドを落とさず、未到達の分岐を探して、テストを足すための材料として使っている（フロントは、分岐カバレッジを見る設定、バックエンドは、未到達分岐を拾うCSVを出力）。

---

### Q31. フロントエンドテスト戦略

#### 📝 問題

> Testing Libraryの思想と、その利点を説明してください。

#### ✅ 回答

**Testing Libraryの思想**
「テストは、ソフトウェアの使われ方に、似ているほど、自信を与えてくれる」という考え方。コンポーネントの内部の作り（クラス名、状態、構造）ではなく、ユーザーが、実際に見て操作するもの（ボタンという役割と名前、表示される文字、ラベルなど）を通して、テストする。

**利点**
1. **リファクタリングに強い**: クラス名の変更や、コンポーネントの分割をしても、画面の見た目と動きが同じなら、テストは壊れない。内部の実装ではなく、外から見える動きを確かめているため。
2. **実際の不具合を見つけやすい**: ユーザーの操作に近い流れで確かめるため、「押せない」「表示されない」など、実際にユーザーが困る問題を見つけられる。
3. **アクセシビリティも保てる**: `getByRole` で、役割と名前から要素を探すため、スクリーンリーダーでも使える、正しいマークアップを保つことにつながる。
4. **非同期の処理を自然に扱える**: `findBy...` で、要素が現れるまで待つなど、ユーザーが「表示されるのを待つ」動作に近い形で書ける。

**このアプリでの実装**
Vitest、jsdom、Testing Library（`@testing-library/react`、`user-event`、`jest-dom`）で、テストしている。`getByRole`（126回）、`getByLabelText`（35回）、`getByText`（41回）が中心で、クラス名に頼る書き方（`querySelector`）は0回。`PostActions.test.tsx` では、「いいね」という名前のボタンを探し、`userEvent.click` でクリックして、`aria-pressed` や無効状態を確かめている。

---

## 🧭 第6章: 設計原則と実務判断（5問）

### Q32. N+1問題

#### 📝 問題

> 概念と、タイムライン例で説明してください。

#### ✅ 回答

**N+1問題とは**
一覧を取得する1回のクエリのあとに、その一覧のN件それぞれについて、関連するデータを取得するクエリを、N回、追加で発行してしまい、合計でN+1回のクエリが走る問題。DBへの問い合わせの回数が、件数に比例して増え、通信の往復が積み重なって、表示が遅くなる。開発中の少ないデータでは気づきにくく、本番でデータが増えてから、問題になりやすい。

**タイムラインの例**
タイムラインに、投稿が20件表示されるとき、まず、投稿を取得するSQLを1回発行し、そのあと、20件の投稿それぞれについて、投稿者の情報を取得するSQLを20回発行すると、合計21回になる。100件なら101回、50件なら51回になる。

**解決策**
①JOINで、投稿と投稿者を、1本のSQLで一度に取得する。②投稿IDを集めて、`IN` で、関連データ（画像など）を、まとめて1回で取得し、投稿IDごとに振り分ける。

**このアプリでの実装**
`PostMapper.findTimeline` が、`JOIN users` で、投稿と投稿者を、1本のSQLで取得し、画像は、`PostService.toTimeline` で、`postImageMapper.findByPostIds` を使って、投稿IDをまとめて、1回で取得している。ページの件数によらず、発行するSQLは2本。MyBatisは、SQLを自分で書くため、発行されるクエリの数が、コードから見えやすい。

---

### Q33. ページネーション

#### 📝 問題

> 必要な理由と、代表的な2方式を比較してください。

#### ✅ 回答

**ページネーションが必要な理由**
大量のデータを、一度に全部返すと、通信量とサーバーの負荷が増え、画面の表示も遅くなる。一定の件数ずつ区切って返すことで、これらを防げる。

**代表的な2方式の比較**

| 観点 | オフセット方式 | カーソル方式 |
|---|---|---|
| 指定 | 「何件目から何件」（`OFFSET` / `LIMIT`） | 「最後に見たIDの次から何件」（`WHERE id < 最後のID`） |
| データの増減 | 先頭に追加されると、全体がずれて、重複や飛ばしが起きる | 目印から始めるので、ずれない |
| ページが深いとき | 先頭から読み飛ばすので、遅くなる | 深さに関係なく速い |
| ページ番号でのジャンプ | できる | できない |
| 向く画面 | 検索結果、管理画面など | 無限スクロールのタイムラインなど |
| 実装 | 簡単 | やや複雑 |

**このアプリでの実装**
タイムラインは、新しい投稿が増え続け、下へスクロールして続きを読む画面のため、カーソル方式を採用している。`findTimeline` が、`WHERE id < #{cursor} ORDER BY id DESC LIMIT` で、最後に見た投稿のIDより、古いものを取得する。Serviceは、1件多く取得して、次のページの有無（`hasNext`）を判定し、最後の投稿のIDを `nextCursor` として返す。フロントの `useCursorPager` が、そのカーソルで、続きを読み込む。

---

### Q34. 画像アップロード設計

#### 📝 問題

> DB保存とS3保存の違いと利点を説明してください。

#### ✅ 回答

**DB保存とS3保存の違い**
DB保存は、画像の中身（バイナリ）を、DBのカラムに直接保存する方法。S3保存は、画像の中身を、ファイル用のストレージ（S3）に保存し、DBには、場所を示すキー（`s3_key`）だけを記録する方法。

**S3保存の利点**
1. **DBの負担が軽い**: 画像は、1枚で数MBになることもあり、DBに保存すると、DBが肥大化して、バックアップ・復元に時間がかかり、検索や他の処理も遅くなる。
2. **容量・コスト・耐久性**: S3は、ファイル保存に特化しており、容量を気にせず、安価に保存でき、耐久性も高い（11ナイン）。
3. **配信の負荷を減らせる**: 署名付きURLを使えば、ブラウザがS3から直接画像を取得でき、サーバー（Spring Boot）が画像を中継しなくてよい。
4. **複数のサーバーでも、同じ画像を見られる**: EC2のディスクに保存すると、台数が増えたときに、画像が台ごとにバラバラになるが、S3なら、全台が同じ場所を見る。

**このアプリでの実装**
`post_images` テーブルには、`s3_key` だけを持たせ、画像本体は、S3に保存している。S3は、公開アクセスを全遮断し（Terraformで定義）、期限つきの署名付きURLで配信する。アップロードは、`ImageValidator` が、1枚2MBまで、寸法4096px以内、JPEG・PNGのみを、実際の中身から判定して検証し（申告された `Content-Type` や拡張子は信用しない）、1投稿あたり最大4枚を、DBの制約でも守っている。

画像を公開にすると、URLを知っていれば、誰でも、いつまでも見られ、URLが漏れても取り消せず、想定外のアクセスで通信料金も膨らむ。署名付きURLなら、期限つきで、アプリが許可した相手にだけ発行でき、期限が切れれば使えなくなる。

---

### Q35. APIバージョニング

#### 📝 問題

> 代表的な方式と特徴を説明してください。

#### ✅ 回答

**APIバージョニングとは**
APIの仕様を、破壊的に変更するとき（項目名の変更や削除など）、古い仕様を期待しているクライアントを壊さないよう、新旧を区別できるように、バージョンを付けて管理すること。項目の追加は、古いクライアントを壊さないが、項目名の変更や削除は壊す。

**代表的な方式と特徴**

| 方式 | 例 | 特徴 |
|---|---|---|
| URLパス方式 | `/api/v1/users` | 最も分かりやすく、URLを見ればバージョンが分かり、ブラウザやcurlでも試しやすい。最も広く使われる。一方、同じリソースなのに、URLが変わる |
| クエリパラメータ方式 | `/api/users?version=2` | 簡単に付けられ、パスは変わらない。付け忘れたときの既定のバージョンを決める必要がある。キャッシュとの相性が悪くなることがある |
| ヘッダー方式 | `Accept: application/vnd.myapp.v2+json` | URLが変わらず、RESTの考え方に近い。一方、ブラウザで見えにくく、実装やテストがやや複雑 |

どの方式でも、バージョンを増やすと、古いバージョンを保守し続ける負担が増えるため、なるべく破壊的な変更を避けるのが基本方針。

**このアプリでの考え方**
APIのURLにバージョン番号は付けていない。APIを使うのは、自分たちのフロントエンドだけだが、フロントとバックエンドは別々にデプロイされ、一時的にバージョンがずれるため、互換性を保つ運用ルールで対応している（`docs/09_infrastructure.md`）。バックエンド → フロントエンドの順でデプロイし、フィールドの追加はよいが、削除・リネームは、1回のデプロイで行わず、新旧の両方を返す期間を挟む。外部への公開や、スマホアプリなど、古い版が長く残る場合は、URLパス方式などのバージョニングが必要になる。

---

### Q36. 技術選定

#### 📝 問題

> 判断基準を説明し、
>
> React + Spring Boot + PostgreSQL を例に具体化してください。

#### ✅ 回答

**技術選定の判断基準**
流行りではなく、基準に基づいて選ぶ。
1. **要件に合うか**: 作りたいものに、必要な機能があるか。
2. **チームのスキル・学習コスト**: 使えるか、習得にかかる時間と、バランスが取れるか。
3. **エコシステム・情報量**: ライブラリや情報が豊富で、困ったときに調べやすいか。
4. **保守性・将来性**: 開発が続いていて、長く使い続けられるか。
5. **性能・スケーラビリティ**: 必要な負荷に耐えられ、増やせるか。
6. **セキュリティ**: 脆弱性への対応が早く、バージョンアップが続いているか。
7. **コスト**: ライセンスや運用の費用のバランス。
8. **実績・安定性**: 世間で十分に使われ、安定しているか。

**React + Spring Boot + PostgreSQL での具体化（SNSアプリ）**
- **React（+ TypeScript）**: コンポーネント、Context、カスタムフックなど、設計の道具が豊富で、エコシステムと情報が多い。TypeScriptとの相性が良く、APIの型をOpenAPIから自動生成でき、AIが書いたコードのレビューにも有利。
- **Spring Boot**: Spring Security（JWT認証、CORS、BCrypt）、例外の一括処理、Flyway、MyBatis連携、Actuatorなど、必要な機能が標準で揃い、実績があり、保守も継続している。レイヤードアーキテクチャとも相性が良い。
- **PostgreSQL**: ユーザー・投稿・フォロー・いいね・コメントなど、互いに関連するデータを扱うため、JOIN、外部キー、UNIQUE・CHECK制約、トランザクションを持つリレーショナルDBが向く。MongoDBのようなNoSQLでは、JOINや外部キー・CASCADE、制約のような、DBが関連の整合性を守る仕組みが、自然には使えず、アプリ側で書く必要が増える。オープンソースでライセンス費用がかからず、AWSのRDSで、マネージドに運用できる。

また、このアプリは学習目的のため、業務で広く使われ、学ぶ価値が高いことも観点になる。技術選定に唯一の正解はなく、前提（規模、チーム、要件）が変われば答えも変わるため、基準を使って、なぜそれを選んだかを、説明できることが大切。

---

以上、全36問。
