# Claude Code の設定はローカル固有（絶対パスを含む）なので共有しない。
# ただし skills/ は手順書としてチームで共有しレビュー対象にするため追跡する
.claude/*
!.claude/skills/
.DS_Store

# 環境変数（機密情報を含むためコミットしない）
.env

# バックエンド
backend/build/
backend/.gradle/

# フロントエンド
frontend/node_modules/
frontend/dist/
frontend/.env
frontend/.env.local

# Terraform（state はアカウント ID 等を含むためコミットしない。
# .terraform.lock.hcl はバージョン固定のため追跡する）
terraform/.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars
*.auto.tfvars
crash.log
crash.*.log
*.tfplan

# パフォーマンステストの結果（実行のたびに増える。比較用の数値は docs/13 に転記する）
perf/results/*
!perf/results/.gitkeep

# 実サイズの画像でアップロードを測りたいときに置く任意のファイル（perf/README.md）
perf/k6/assets/
